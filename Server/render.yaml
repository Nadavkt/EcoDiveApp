services:
  - type: web
    name: ecodive-server
    env: node
    region: oregon
    plan: free
    rootDir: Server
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      # Set these in Render Dashboard with actual values
      # - key: DATABASE_URL
      #   sync: false
      # - key: HF_TOKEN
      #   sync: false
      # - key: HF_MODEL
      #   value: HuggingFaceH4/zephyr-7b-beta
      # - key: ALLOWED_ORIGINS
      #   value: https://your-app.web,https://your-app.onrender.com

